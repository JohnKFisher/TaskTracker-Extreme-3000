# Node / npm
node_modules/

# Tauri / Rust build artifacts
src-tauri/target/
src-tauri/gen/

# Build output
dist/

# User data — contains API keys, tasks, notes, config (never commit)
data/

# Machine-specific settings — contains sync folder path (never commit)
local-settings.json

# Electron builder cache (kept in case branch is rebased)
.cache/
build/

# Claude Code local settings (machine-specific paths)
.claude/

# OS files
.DS_Store
Thumbs.db
